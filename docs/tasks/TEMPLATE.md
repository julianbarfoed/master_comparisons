# Task: <short name>

Fill this in when launching an agent. It can be supplied directly in the launch
prompt or saved as a task document; an unfilled template is not an assignment.

## Goal and scope

- Desired behavior:
- Constraints and technology choices, if decided:
- Out of scope:

## Ownership

- Branch and worktree:
- Owned files/modules and tests:
- Shared files handled by the coordinator or another named owner:

## Interfaces and dependencies

- Inputs/outputs this task exposes, including errors:
- Interfaces consumed from other agents:
- Shared definitions or decisions to follow:
- What can be mocked while dependencies are in progress:

## Acceptance and handoff

- Observable outcomes required for completion:
- Checks to run, including integration checks when dependencies are ready:
- Commit/push/PR instructions and review target:

Read `AGENTS.md`. Implement this assignment in your worktree. Coordinate changes
to shared interfaces, run relevant checks, and report results, assumptions, and
remaining dependencies. Leave merging to the user.
